/**
 * DEPRECIADO. O backend de extração via pdfjs-dist causava SIGBUS neste
 * ambiente. A extração agora é feita por src/extract/extract_pdf.py
 * (pdfplumber) e orquestrada por src/extract/index.ts.
 *
 * Este arquivo é mantido apenas como stub para não quebrar histórico.
 */
export {};
